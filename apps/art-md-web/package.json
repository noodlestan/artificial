{
  "name": "apps-art-md-web",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "lint": "prettier . -c",
    "lint:fix": "prettier . -c --write",
    "build:clean": "rm -rf dist",
    "build": "astro build",
    "test": "echo none yet",
    "preview": "astro preview",
    "start": "astro",
    "ci": "npm run lint && npm run build && npm run test"
  },
  "dependencies": {
    "@art-md/codec": "^0.0.2",
    "@astrojs/solid-js": "^7.0.2",
    "astro": "^7.2.7",
    "solid-js": "^1.9.15"
  },
  "allowScripts": {
    "esbuild": true
  }
}
