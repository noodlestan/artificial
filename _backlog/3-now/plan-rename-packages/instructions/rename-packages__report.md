# Sub-Agent REPORT (#producer)

**Plan:** `rename-packages`

**Instruction Id:** `rename-packages`

**Outcome:** `COMPLETED`

## Evidence

### Changes

| Instruction Goal | Status | Evidence |
| --- | --- | --- |
| Singularize `@art-js/artificials-*` → `@art-js/artificial-*` package names in `package.json` | COMPLETED | 8 `package.json` names singular (`art-js/spec`, `art-js/libs/{primitives,parser,validator,bundler,program}`, `art-js/cli/{bin,dev-server}`); `art-js/cli/language-server` has NO `package.json` (only a README); `art-js/cli/watcher` stays `@art-js/artificials-watcher`. |
| Rename package records in `ops/records/packages/` | COMPLETED | 10 records renamed via `git mv` (`artificials-{x}.art` → `artificial-{x}.art`), each with `## Package: Artificial {X}` heading and singular `**Canonical Name:**`; poc-parse keeps `@art-js/poc-parse`. `artificials-watcher.art` untouched. |
| Rename the `artificials-lib-build.art` script-set record and its lib-record references | COMPLETED | `ops/records/scripts/artificials-lib-build.art` → `artificial-lib-build.art`, heading `## Package Script Set: Artificial Lib Build`; lib records (primitives, parser, validator, bundler, program) now reference `Artificial Lib Build`; `artificials-cli-build.art` untouched, CLI records keep `Artificials Cli Build`. |
| Singularize namespace references (except Watcher) | COMPLETED | `ops/records/namespaces/art-js.art` — 9 `- Package: Artificial {X}`; `- Package: Artificials Watcher` stays. |
| Singularize docs references and display names | COMPLETED | Root `README.md` table (8 rows; watcher row stays), `art-js/README.md` link texts (Watcher stays), README h1s (primitives, parser, validator, program, bundler, bin, dev-server, language-server, tools), `art-js/libs/parser/architecture/index.md`, `architecture/records/adr/language.art`. |
| Regenerate `package-lock.json` via `npm install` | COMPLETED | Lockfile regenerated at repo root; singular workspace entries; never hand-edited. |
| Keep exclusions untouched | COMPLETED | `_backlog/**`, `art-domains/**`, `artisans/**`, `_temp/**`, `art-js/cli/poc-parse/**`, `art-js/cli/watcher/**`, `artificials-watcher.art`, `artificials-cli-build.art`, `cli-dev.art`, `_guide.md`, `_architect.md`, `_parking-lot.md`, `reference/**` all untouched. |

#### Files changed

- `package.json` (8): `art-js/spec/`, `art-js/libs/primitives/`, `art-js/libs/parser/`, `art-js/libs/validator/`, `art-js/libs/bundler/`, `art-js/libs/program/`, `art-js/cli/bin/`, `art-js/cli/dev-server/` — `"name": "@art-js/artificials-*"` → `"@art-js/artificial-*"`.
- `package-lock.json` — regenerated by `npm install` (singular workspace entries; `@art-js/artificials-watcher` retained).
- Package records (10, renamed + content): `ops/records/packages/artificial-{primitives,parser,validator,bundler,program,bin,dev-server,language-server,tools,poc-parse}.art` — heading + canonical name singularized; lib records also updated `Package Script Set: Artificials Lib Build` → `Artificial Lib Build`.
- Script-set record (1, renamed + content): `ops/records/scripts/artificial-lib-build.art` — heading singularized.
- Namespace record: `ops/records/namespaces/art-js.art` — 9 package references singularized (Watcher kept).
- Docs references: `README.md` (root package table), `art-js/README.md` (link texts), `art-js/libs/parser/architecture/index.md` (`@art-js/artificial-primitives`), `architecture/records/adr/language.art` (`@art-js/artificial-spec`).
- README display names (h1s): `art-js/libs/{primitives,parser,validator,program}/README.md` (`# artificials-{x}` → `# artificial-{x}`), `art-js/libs/bundler/README.md` (`# Artificials Bundler` → `# Artificial Bundler`), `art-js/cli/{bin,dev-server,language-server,tools}/README.md` (singular `# Artificial {X}`). Toolkit `[@artificials]` links and `artificials-build` mentions left as-is.

**Verification:**

- Scoped grep (`grep -rn "artificials-" --include="*.md" --include="*.art" --include="*.json" --include="*.ts" .` minus node_modules/lockfile/.git): every remaining hit is on the expected-residue list — `_backlog/`, `art-domains/`, `art-js/cli/poc-parse/`, `art-js/cli/watcher/`, `ops/records/scripts/artificials-cli-build.art`, `ops/records/packages/artificials-watcher.art`, the root README watcher row (stays by instruction), and the `artificials-build` / `artificials-watch` mentions in `art-js/**/README.md`.
- Sanity spot-checks: (1) `grep "artificials-" art-js/libs/primitives/package.json art-js/libs/parser/package.json art-js/spec/package.json` → nothing; (2) `ls ops/records/packages/ | grep artificial` → 10 `artificial-*.art` + `artificials-watcher.art`; (3) `grep "Package: Artificials "` on namespace + renamed records → only `- Package: Artificials Watcher`.
- `npm run lint` → 11/11 tasks successful, exit 0. `npm run lint:fix` → no formatting changes needed.
- Commit: `2f2f326` `refactor: singularize artificial package names`, pushed `cdac73e..2f2f326` to `origin/main`. Lefthook hooks (`1-extract`, `2-ci`) passed.

## Blockers (if any)

None.

## Feedback

### For the planner

Three discrepancies between the instruction's inventory and the live tree — all resolved with the simplest reading (no code-only fixes):

- **where:** `## Changes` item 1 and `Step 2` (`art-js/cli/language-server/` listed among the 10 `package.json` renames); also `plan.md` `## Scope > Packages` ("10 packages") and commit scope.
- **problem:** `art-js/cli/language-server/` contains no `package.json` (only `README.md`; confirmed via `git ls-files`). Only 8 `package.json` files actually exist to rename (spec, primitives, parser, validator, bundler, program, bin, dev-server), not 9-in-scope / 10.
- **decision:** Renamed the 8 existing `package.json` names; nothing to rename for language-server.
- **READY-TO-APPLY snippet for `_backlog/3-now/plan-rename-packages/plan.md`:**

  ```text
  - Rename `@art-js/artificials-*` → `@art-js/artificial-*` in `package.json` names (8 packages: spec, primitives, parser, validator, bundler, program, bin, dev-server; language-server has NO package.json, only a README; watcher excluded).
  ```

- **where:** `Step 3` item 5 sanity check — `grep -rn "Package: Artificials " ops/records/` expected to return "only the watcher record, the cli-build script-set record, and the cli-dev dependency record".
- **problem:** The literal pattern `"Package: Artificials "` cannot match `## Package Script Set: Artificials Cli Build` or `## Package Dependency: Artificials Build Tools`; the actual output is stricter (only the watcher namespace reference and the `artificials-watcher.art` heading). The instruction's `## Verification` sanity check (namespace + renamed records only) states the correct expectation.
- **decision:** Implemented the stricter actual result, which satisfies both checks.
- **READY-TO-APPLY snippet for `_backlog/3-now/plan-rename-packages/plan.md`:**

  ```text
  - Sanity: `grep -rn "Package: Artificials " ops/records/` must return only the watcher references (`- Package: Artificials Watcher` in the namespace and `## Package: Artificials Watcher` in the record); the cli-build script-set (`Package Script Set: Artificials Cli Build`) and cli-dev dependency (`Package Dependency: Artificials Build Tools`) don't match that literal pattern.
  ```

- **where:** `Step 4` item 1 — "singularize the 9 renamed rows" in the root `README.md` package table.
- **problem:** The table holds 8 `@art-js/artificials-*` rows to rename; the 9th plural row is watcher (excluded, stays). "9 renamed rows" overcounts by one.
- **decision:** Singularized the 8 rows; watcher row kept.
- **READY-TO-APPLY snippet for `_backlog/3-now/plan-rename-packages/plan.md`:**

  ```text
  - Root `README.md` package table: singularize the 8 renamed rows (`@art-js/artificials-*` → `@art-js/artificial-*`); the `@art-js/artificials-watcher` row stays.
  ```

### For the technical writers

- The expected-residue lists (`## Verification`, `## Final Verification`) don't mention the root `README.md` watcher row (`@art-js/artificials-watcher`), but the body (`## Changes` item 5, `Step 4` item 1) explicitly keeps it plural. It is intentional residue; consider adding it to the residue list so the scoped-grep expectation is exhaustive.
- `art-js/cli/language-server/` (record + README exist, no package) and `art-js/cli/tools/` (README + record exist, no package) are doc-only packages; the plan notes this only for tools. Worth an explicit line for language-server too.

### For the crew

- None — the rename is self-contained and verification was deterministic (single grep, two spot-checks, lint).
