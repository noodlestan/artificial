# Sub-Agent REPORT (#producer)

**Plan:** `workspace-cli`

**Instruction Id:** `workspace-cli-install`

**Outcome:** `COMPLETED`

## Evidence

### Changes

The goal was met: a workspace root `package.json` now exists with `@art-domains/workspace-cli@0.0.1` as its single devDependency, and the CLI is exposed and verified via npm scripts.

| Instruction Goal                                                                  | Status | Evidence                                                                                                                                                                          |
| --------------------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Install `@art-domains/workspace-cli@0.0.1` in workspace root                      | DONE   | `package.json` created with the CLI as the single devDependency; `npm install` succeeded (9 packages added, 0 vulnerabilities); `node_modules/@art-domains/workspace-cli` present |
| Expose CLI via npm scripts (`workspace`, `workspace:sanity`, `workspace:publish`) | DONE   | `scripts` block added to `package.json`; confirmed by reading the manifest                                                                                                        |
| Verify CLI accessible via `npm run`                                               | DONE   | `npm run workspace -- --help` exit 0; `npm run workspace -- --version` → `0.0.1`; `npm run workspace:sanity` → `sanity command - TODO` (exit 0)                                   |

#### Files changed

- `package.json` — created at workspace root (`name: noodlestan-workspace`, `private: true`), with `scripts` (`workspace`, `workspace:sanity`, `workspace:publish` → `art-workspace` …) and the single `devDependencies` entry `@art-domains/workspace-cli: 0.0.1`.
- `package-lock.json` — generated by `npm install`; locks the workspace root dependency tree (the CLI + its transitive `commander` / `simple-git` deps).

No other manifests or configuration files were created.

## Blockers (if any)

None. All steps and validation commands completed successfully.

## Feedback

### For the planner

- **Where:** `## Rules` (last rule: "Do NOT create any other manifests or configuration files").
- **Problem:** `npm install` (mandated in Step 1) automatically generates `package-lock.json` at the workspace root. The rule does not state whether this lockfile is expected, should be committed, or should be gitignored — it reads as if no second manifest may exist.
- **Decision:** Kept and committed `package-lock.json` as the natural, reproducible by-product of the mandated install, since the workspace is `private: true` and the lockfile pins the CLI install.
- **Ready-to-apply snippet for the plan file:**

```markdown
- RULE: `npm install` generates a `package-lock.json` at the workspace root; commit it as the
  deterministic lock of the workspace root dependency tree.
```

- **Where:** `## Goals` and Step 1 (authorise creating `package.json`).
- **Problem:** No ambiguity in the current revision, but a previous revision was blocked because the instruction presumed an existing root `package.json`. The current revision fixes this; noting it so the fix is not regressed in future instructions.
- **Decision:** Executed the authorised creation exactly as specified (name, private, single devDependency), keeping the declared scope.

### For the technical writers

- **Where:** `ops/_architect.md` (## How → Conventions, ## Architecture) and `ops/records/workspace.art` (## Workspace: Noodlestan → Layout).
- **Problem:** Both files describe the workspace as a meta-repo that is "**not a build root**", but neither records the root `package.json` as a declared workspace artefact. The workspace now carries a root manifest (devDependency + npm scripts), so the records are out of date.
- **Ready-to-apply snippet for `ops/records/workspace.art`:**

```markdown
- `package.json` — workspace root manifest (declared 2026-08-09): holds the
  `@art-domains/workspace-cli` devDependency and the `workspace*` npm scripts.
```

- **Ready-to-apply snippet for `ops/_architect.md` (## How → Conventions):**

```markdown
- The workspace root carries a `package.json` (private) holding `@art-domains/workspace-cli`
  as its single devDependency; it is a tooling manifest, not a build root manifest.
```

### For the crew

- Developer experience: a single ambiguity cost a full delegation cycle previously (the missing `package.json` was misread as a blocker). Declaring which files are expected to pre-exist — and which are created by the instruction — in the plan commit avoids wasted rounds. This instruction's explicit authorisation to create `package.json` is the right shape; keeping it prevents regressions.
